# REPL

<b>Read-Eval-Print-Loop</b>

* Type `node` in a terminal
* Everything we have seen is usable since the repl!

##--##

# Some commands

* .help
* .editor
* .save
* .load
* .exit (`<ctrl>` + D)
* .break (`<ctrl>` + C)
* `<tab>`

Notes:
- Make a demo
- explore the globals
