# Le REPL

<b>Read-Eval-Print-Loop</b>

* Taper `node` dans un terminal
* Tout ce qu’on a vu est utilisable depuis le repl !

##--##

# Quelques commandes

* .help
* .editor
* .save
* .load
* .exit (`<ctrl>` + D)
* .break (`<ctrl>` + C)
* `<tab>`
